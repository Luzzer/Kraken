---
summary: "CLI reference for `uagent clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `uagent clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `uagent clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `uagent clawbot qr` (same behavior as [`uagent qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `uagent clawbot qr` -> `uagent qr`

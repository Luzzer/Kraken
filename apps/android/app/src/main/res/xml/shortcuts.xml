<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_UAGENT"
        app:queryPatterns="@array/ask_uagent_query_patterns">
        <intent
            android:action="ai.uagent.app.action.ASK_UAGENT"
            android:targetPackage="ai.uagent.app"
            android:targetClass="ai.uagent.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>
